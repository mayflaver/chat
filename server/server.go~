package main

import (
	"net"
)

func main() {
	ln, err := net.Listen("tcp", ":8080")
	if err != nil {
		// handle error
	}
	buf := make([]net.Conn, 8)
	for {
		conn, err := ln.Accept()
		if err != nil {
		// handle error
			continue
		}
		conn.Write([]byte("hello"))
	}
}
